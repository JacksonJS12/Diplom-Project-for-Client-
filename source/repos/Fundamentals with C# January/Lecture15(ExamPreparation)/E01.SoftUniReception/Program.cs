using System;

namespace E01.SoftUniReception
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int firstEmployeeEfficiency = int.Parse(Console.ReadLine());
            int secondEmployeeEfficiency = int.Parse(Console.ReadLine());
            int thirthEmployeeEfficiency = int.Parse(Console.ReadLine());
            int studentsCount = int.Parse(Console.ReadLine());

            int totalEmployeeEfficiencyPerHour = firstEmployeeEfficiency + secondEmployeeEfficiency + thirthEmployeeEfficiency;

            int hours = 0;
            for (int i = studentsCount; i <= length; i--)
            {

            }
                studentsCount -= totalEmployeeEfficiencyPerHour;
                hours++;

                studentsCount = int.Parse(Console.ReadLine());
            
            Console.WriteLine(hours);
        }
    }
}
